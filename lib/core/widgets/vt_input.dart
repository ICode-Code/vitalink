import 'package:flutter/widgets.dart';

class VtInput extends StatelessWidget {
  const VtInput({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Input")));
  }
}
